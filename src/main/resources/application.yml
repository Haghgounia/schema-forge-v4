spring:
  application:
    name: schema-forge-v4
  datasource:
    url: ${SCHEMAFORGE_DB_URL:jdbc:h2:mem:schemaforge}
    username: ${SCHEMAFORGE_DB_USER:sa}
    password: ${SCHEMAFORGE_DB_PASSWORD:}
    driver-class-name: ${SCHEMAFORGE_DB_DRIVER:org.h2.Driver}

server:
  port: ${SERVER_PORT:8080}
