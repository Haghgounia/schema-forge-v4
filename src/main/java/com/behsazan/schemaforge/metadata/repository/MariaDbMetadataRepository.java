package com.behsazan.schemaforge.metadata.repository;

/** MariaDB-specific metadata contract used by live comparison and later migration generation. */
public interface MariaDbMetadataRepository extends MetadataRepository {
}
