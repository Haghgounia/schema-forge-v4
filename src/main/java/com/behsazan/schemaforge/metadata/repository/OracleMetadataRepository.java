package com.behsazan.schemaforge.metadata.repository;

/** Oracle-specific metadata contract. More Oracle metadata operations can be added here in later phases. */
public interface OracleMetadataRepository extends MetadataRepository {
}
