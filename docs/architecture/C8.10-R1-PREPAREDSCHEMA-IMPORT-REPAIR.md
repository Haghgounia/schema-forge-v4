# SchemaForge V4 - C8.10-R1 PreparedSchema Import Compile Repair

**Status:** REPAIR CANDIDATE / PENDING USER RE-RUN  
**Official baseline:** `SCHEMAFORGE-V4-CONSOLIDATED-BASELINE-20260823-C8.9`

## Failure evidence

The first C8.10 targeted Maven command stopped during main compilation before Surefire:

```text
Compiling 276 source files
SchemaForgeApiService.java:[159,9] cannot find symbol
  symbol: class PreparedSchema
BUILD FAILURE
Finished at: 2026-08-23T06:03:25-07:00
```

## Root cause

C8.10 correctly moved Standard/Legacy single-document packaging into `ArtifactGenerationService`, but import cleanup removed `PreparedSchema` from `SchemaForgeApiService`. The EA facade path still intentionally declares a `PreparedSchema` local returned by `EaGenerationOrchestrator.prepare(...)`.

## Repair

Exactly one production line is restored:

```java
import com.behsazan.schemaforge.application.PreparedSchema;
```

No method body or test source changes. No behavior change.

## Impact

- DBMS: none; Oracle, PostgreSQL, Db2/zOS, SQL Server and MySQL generation are unchanged.
- Parsers: unchanged.
- DDL/migration/comparison/CRUD/diagram: unchanged.
- C5 naming and layout: unchanged.
- C6 manifest: unchanged.
- C7 REST contract: unchanged.
- Ledger and ZIP contents: unchanged.

## Identity

```text
Main Java                 : 276
Test Java                 : 189
Expected targeted         : 43 / 0 / 0 / 0
Expected full             : 554 / 0 / 0 / 4
R1 source fingerprint     : 03d01cfd60e6b04be02ecc8df9c0dd6c47d6f06dc07f77f6c60844a93d5102ba
```

## Promotion rule

C8.9 remains official. C8.10-R1 can close C8 only after targeted and full Maven gates pass on the user environment.
