# R8.1 — Pre-Freeze Preparation

**Date:** 2026-08-26  
**Status:** VERIFIED / CLOSED PRE-FREEZE RELEASE CANDIDATE / NOT FINAL OPERATIONAL FREEZE  
**Production logic change:** none

R8.1 prepares SchemaForge V4 for the final R8 operational baseline without changing parser, canonical model, DDL, metadata, comparison, migration, REST, or artifact-generation semantics.

## Scope

R8.1 freezes the evidence and packaging structure accumulated after the C11 consolidated baseline:

- R7.1 legacy evidence-backed recovery closure;
- R7.2 five-DBMS recovered-corpus acceptance;
- R7.3 standard Word corpus acceptance with SAFE and OPTIMIZED strict gates;
- R7.4 live M2 acceptance evidence for SQL Server, MySQL, PostgreSQL, and Oracle;
- explicit Db2 z/OS live state `PENDING_ENVIRONMENT`, not PASS and not FAIL;
- known blockers and deferred scope;
- source/test inventory and a repeatable final full-regression command.

## Source inventory

The R8.1 verified candidate is based on the R7.3.3 strict-gate production source package. Production source remains unchanged; test-side regression contracts were aligned during R8.1 closure.

```text
Project version              : 4.0.0-SNAPSHOT
Java release                 : 21
src/main/java .java files    : 280
src/test/java .java files    : 195
Total Java source/test files : 475
```

No Java production source is changed by R8.1. During pre-freeze regression, seven test-side files were aligned with already-frozen R7.3 fail-closed semantics and current service overloads. These changes do not alter `src/main` runtime behavior.

## Acceptance state entering R8.1

| Stage | State | Evidence summary |
|---|---|---|
| C0-C11 Consolidation | CLOSED / FROZEN | C11 targeted 95/0/0/0; full clean 554/0/0/4 |
| R7.1 Legacy Recovery | CLOSED / PASS | 5321 discovered/loaded; 4704 generated; 617 hard-blocked; 0 read/generation failures |
| R7.2 Legacy five-DBMS corpus | CLOSED / PASS | SAFE + OPTIMIZED strict corpus gates accepted |
| R7.3 New Word corpus | CLOSED / PASS | 660/660 standard parser; SAFE Strict PASS; OPTIMIZED Strict PASS |
| R7.4 SQL Server live | PASS | M2 live pilot; 20 statements; residual 0; data preserved |
| R7.4 MySQL live | PASS | M2 live pilot; 14 statements; residual 0; data preserved |
| R7.4 PostgreSQL live | PASS | M2 live pilot; 16 statements; residual 0; data preserved |
| R7.4 Oracle live | PASS | M2 live pilot; 16 statements; residual 0; data preserved |
| R7.4 Db2 z/OS live | PENDING_ENVIRONMENT | live Db2 z/OS access is not currently available |

R7.4 therefore remains **OPEN (4/5 live DBMS passed)**. R8.1 does not reinterpret the missing Db2 environment as a pass.

## R7.3 frozen Word baseline

```text
Documents       : 660
Skipped         : 0
Parse failures  : 0
Parser mode     : standard

Oracle          : 656 generated / 0 with issues / 4 failed
PostgreSQL      : 657 generated / 0 with issues / 3 failed
Db2 z/OS        : 656 generated / 0 with issues / 4 failed
SQL Server      : 656 generated / 0 with issues / 4 failed
MySQL           : 644 generated / 0 with issues / 16 failed
```

Both SAFE and OPTIMIZED strict runs use the same accepted counts and exact failure taxonomy. The total 31 target failures are known blockers; there is no accepted generic `GENERATION_FAILED` category.

## R7.4 live baseline

| DBMS | Server | Statements | Residual | Data preserved | Cleanup |
|---|---|---:|---:|---|---|
| SQL Server | Microsoft SQL Server 16.00.4265 | 20 | 0 | true | true |
| MySQL | MySQL 8.4.11 | 14 | 0 | true | true |
| PostgreSQL | PostgreSQL 16.14 | 16 | 0 | true | true |
| Oracle | Oracle AI Database 26ai Free / 23.26.2.0.0 | 16 | 0 | true | true |
| Db2 z/OS | unavailable | — | — | — | — |

Raw summary evidence is retained under `docs/release/evidence/r7.4/`.

## R8.1 regression gate

R8.1 regression verification completed successfully on the user environment with:

```bat
cd /d D:\Projects\schema-forge-v4
scripts\RUN-R8.1-FULL-REGRESSION.cmd
```

The helper performs `mvnw.cmd clean test`, writes the active log outside `target` so Windows `clean` can remove the build tree, copies the completed log to `target\test-reports\r8.1-full-regression.log`, echoes it to the console, and returns Maven's exit code.

Verified result on 2026-08-26:

```text
Tests run : 587
Failures  : 0
Errors    : 0
Skipped   : 4
BUILD SUCCESS
R8.1 FULL REGRESSION: PASS
```

The focused alignment gate also passed `26 / 0 / 0 / 0`. The nine-document local Word regression reports `8 Passed / 1 Blocked / 0 Failed`; the single block is the known `BIM.PROVINCES.POPULATION` unresolved source datatype and no executable DDL is generated for it.

## Exit criteria for R8.1

R8.1 preparation is complete when:

1. the RC package and SHA-256 are produced;
2. source/test inventory is recorded;
3. R7.1-R7.4 evidence matrix is recorded;
4. known limitations and deferred scope are frozen;
5. the exact RC passes `clean test` on the user environment.

All five R8.1 exit criteria are satisfied. R8.1 is **VERIFIED / CLOSED**. The result remains a pre-freeze release candidate, not the final R8.2 operational baseline.

## R8.2 final freeze dependency

R8.2 requires:

1. actual Db2 z/OS live execution of `Db2ZosMigrationM2LivePilotIT`;
2. `Residual changes = 0` and data preservation for that live pilot;
3. final clean regression after Db2 evidence is recorded;
4. final source/package hashes;
5. promotion to the final R8 Operational Baseline ID.
