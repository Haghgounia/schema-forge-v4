# Phase 19.1 - Runtime Distribution Structure

Status: FROZEN

Validation evidence:

```text
DistributionStructureContractTest
Tests    : 4
Failures : 0
Errors   : 0
Skipped  : 0
Result   : BUILD SUCCESS
Finished : 2026-09-02T19:21:11+03:30
```

SchemaForge V4 4.0.0 GA runtime distribution staging is defined under `distribution/`.

Scope of this phase:

- define the runtime-only package layout;
- keep the validated GA binary identity fixed;
- provide a safe external configuration with no bundled database credentials;
- disable every live metadata repository by default;
- force the Windows launcher to use external configuration through `spring.config.location`;
- provide API smoke and binary checksum verification scripts;
- lock the structure with `DistributionStructureContractTest`.

No production Java source is changed by Phase 19.1.

The validated GA binary to be inserted during Phase 19.3 is:

`bin/schema-forge-v4-4.0.0.jar`

Historical GA SHA-256 recorded before reproducible-build hardening:

`78057619993e942f0a43fb799da754b95282f365b4f6bab09210c86233f6db57`

Phase 19.3 supersedes the hard-coded checksum for distribution assembly with the reproducibly frozen value in `distribution/checksums/SHA256SUMS.txt`.

Phase 19.2 populates the deployment, configuration, DBA, operations and API documentation plus safe runtime samples.
